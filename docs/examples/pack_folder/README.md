# Example Pack Folder

Import this folder through `Add Tool` -> `Import Files` to test folder import.

EditorBinder will read the root `editorbinder-pack.json` once and ignore nested
bundle content during that folder import.
